---
title: CLI
description: Opções e comandos da CLI do papecode.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

A CLI do papecode, por padrão, inicia o [TUI](/docs/tui) quando executada sem argumentos.

```bash
papecode
```

Mas também aceita comandos conforme documentado nesta página. Isso permite que você interaja com o papecode programaticamente.

```bash
papecode run "Explain how closures work in JavaScript"
```

---

### tui

Inicie a interface de usuário do terminal do papecode.

```bash
papecode [project]
```

#### Opções

| Flag         | Curto | Descrição                                                                  |
| ------------ | ----- | -------------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue a última sessão                                                   |
| `--session`  | `-s`  | ID da sessão para continuar                                                |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`) |
| `--prompt`   |       | Prompt a ser usado                                                         |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                              |
| `--agent`    |       | Agente a ser usado                                                         |
| `--port`     |       | Porta para escutar                                                         |
| `--hostname` |       | Nome do host para escutar                                                  |

---

## Comandos

A CLI do papecode também possui os seguintes comandos.

---

### agent

Gerencie agentes para o papecode.

```bash
papecode agent [command]
```

---

### attach

Anexe um terminal a um servidor backend do papecode já em execução, iniciado via comandos `serve` ou `web`.

```bash
papecode attach [url]
```

Isso permite usar o TUI com um backend papecode remoto. Por exemplo:

```bash
# Inicie o servidor backend para acesso web/mobile
papecode web --port 4096 --hostname 0.0.0.0

# Em outro terminal, anexe o TUI ao backend em execução
papecode attach http://10.20.30.40:4096
```

#### Opções

| Flag        | Curto | Descrição                                |
| ----------- | ----- | ---------------------------------------- |
| `--dir`     |       | Diretório de trabalho para iniciar o TUI |
| `--session` | `-s`  | ID da sessão para continuar              |

---

#### create

Crie um novo agente com configuração personalizada.

```bash
papecode agent create
```

Este comando irá guiá-lo na criação de um novo agente com um prompt de sistema personalizado e configuração de ferramentas.

---

#### list

Liste todos os agentes disponíveis.

```bash
papecode agent list
```

---

### auth

Comando para gerenciar credenciais e login para provedores.

```bash
papecode auth [command]
```

---

#### login

O papecode é alimentado pela lista de provedores em [Models.dev](https://models.dev), então você pode usar `papecode auth login` para configurar chaves de API para qualquer provedor que você gostaria de usar. Isso é armazenado em `~/.local/share/papecode/auth.json`.

```bash
papecode auth login
```

Quando o papecode é iniciado, ele carrega os provedores do arquivo de credenciais. E se houver chaves definidas em seus ambientes ou em um arquivo `.env` em seu projeto.

---

#### list

Lista todos os provedores autenticados conforme armazenado no arquivo de credenciais.

```bash
papecode auth list
```

Ou a versão curta.

```bash
papecode auth ls
```

---

#### logout

Desconecta você de um provedor limpando-o do arquivo de credenciais.

```bash
papecode auth logout
```

---

### github

Gerencie o agente do GitHub para automação de repositórios.

```bash
papecode github [command]
```

---

#### install

Instale o agente do GitHub em seu repositório.

```bash
papecode github install
```

Isso configura o fluxo de trabalho necessário do GitHub Actions e o guia pelo processo de configuração. [Saiba mais](/docs/github).

---

#### run

Execute o agente do GitHub. Isso é tipicamente usado em GitHub Actions.

```bash
papecode github run
```

##### Opções

| Flag      | Descrição                                        |
| --------- | ------------------------------------------------ |
| `--event` | Evento simulado do GitHub para executar o agente |
| `--token` | Token de acesso pessoal do GitHub                |

---

### mcp

Gerencie servidores do Model Context Protocol.

```bash
papecode mcp [command]
```

---

#### add

Adicione um servidor MCP à sua configuração.

```bash
papecode mcp add
```

Este comando irá guiá-lo na adição de um servidor MCP local ou remoto.

---

#### list

Liste todos os servidores MCP configurados e seu status de conexão.

```bash
papecode mcp list
```

Ou use a versão curta.

```bash
papecode mcp ls
```

---

#### auth

Autentique-se com um servidor MCP habilitado para OAuth.

```bash
papecode mcp auth [name]
```

Se você não fornecer um nome de servidor, será solicitado que você selecione entre os servidores disponíveis habilitados para OAuth.

Você também pode listar servidores habilitados para OAuth e seu status de autenticação.

```bash
papecode mcp auth list
```

Ou use a versão curta.

```bash
papecode mcp auth ls
```

---

#### logout

Remova credenciais OAuth para um servidor MCP.

```bash
papecode mcp logout [name]
```

---

#### debug

Depure problemas de conexão OAuth para um servidor MCP.

```bash
papecode mcp debug <name>
```

---

### models

Liste todos os modelos disponíveis dos provedores configurados.

```bash
papecode models [provider]
```

Este comando exibe todos os modelos disponíveis entre seus provedores configurados no formato `provider/model`.

Isso é útil para descobrir o nome exato do modelo a ser usado em [sua configuração](/docs/config/).

Você pode opcionalmente passar um ID de provedor para filtrar modelos por esse provedor.

```bash
papecode models anthropic
```

#### Opções

| Flag        | Descrição                                                             |
| ----------- | --------------------------------------------------------------------- |
| `--refresh` | Atualiza o cache de modelos a partir do models.dev                    |
| `--verbose` | Use uma saída de modelo mais detalhada (inclui metadados como custos) |

Use a flag `--refresh` para atualizar a lista de modelos em cache. Isso é útil quando novos modelos foram adicionados a um provedor e você deseja vê-los no papecode.

```bash
papecode models --refresh
```

---

### run

Execute o papecode em modo não interativo passando um prompt diretamente.

```bash
papecode run [message..]
```

Isso é útil para scripts, automação ou quando você deseja uma resposta rápida sem iniciar o TUI completo. Por exemplo.

```bash "papecode run"
papecode run Explique o uso de context em Go
```

Você também pode se anexar a uma instância em execução do `papecode serve` para evitar tempos de inicialização a frio do servidor MCP em cada execução:

```bash
# Inicie um servidor sem cabeça em um terminal
papecode serve

# Em outro terminal, execute comandos que se anexam a ele
papecode run --attach http://localhost:4096 "Explique async/await em JavaScript"
```

#### Opções

| Flag         | Curto | Descrição                                                                     |
| ------------ | ----- | ----------------------------------------------------------------------------- |
| `--command`  |       | O comando a ser executado, use mensagem para argumentos                       |
| `--continue` | `-c`  | Continue a última sessão                                                      |
| `--session`  | `-s`  | ID da sessão para continuar                                                   |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`)    |
| `--share`    |       | Compartilhe a sessão                                                          |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                                 |
| `--agent`    |       | Agente a ser usado                                                            |
| `--file`     | `-f`  | Arquivo(s) a serem anexados à mensagem                                        |
| `--format`   |       | Formato: padrão (formatado) ou json (eventos JSON brutos)                     |
| `--title`    |       | Título para a sessão (usa o prompt truncado se nenhum valor for fornecido)    |
| `--attach`   |       | Anexe a um servidor papecode em execução (por exemplo, http://localhost:4096) |
| `--port`     |       | Porta para o servidor local (padrão para porta aleatória)                     |

---

### serve

Inicie um servidor papecode sem cabeça para acesso à API. Confira a [documentação do servidor](/docs/server) para a interface HTTP completa.

```bash
papecode serve
```

Isso inicia um servidor HTTP que fornece acesso à funcionalidade do papecode sem a interface TUI. Defina `PAPECODE_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `papecode`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### session

Gerencie sessões do papecode.

```bash
papecode session [command]
```

---

#### list

Liste todas as sessões do papecode.

```bash
papecode session list
```

##### Opções

| Flag          | Curto | Descrição                                 |
| ------------- | ----- | ----------------------------------------- |
| `--max-count` | `-n`  | Limitar às N sessões mais recentes        |
| `--format`    |       | Formato de saída: tabela ou json (tabela) |

---

### stats

Mostre o uso de tokens e estatísticas de custo para suas sessões do papecode.

```bash
papecode stats
```

#### Opções

| Flag        | Descrição                                                                                            |
| ----------- | ---------------------------------------------------------------------------------------------------- |
| `--days`    | Mostre estatísticas dos últimos N dias (todo o tempo)                                                |
| `--tools`   | Número de ferramentas a serem mostradas (todas)                                                      |
| `--models`  | Mostre a divisão do uso de modelos (oculto por padrão). Passe um número para mostrar os N principais |
| `--project` | Filtrar por projeto (todos os projetos, string vazia: projeto atual)                                 |

---

### export

Exporte dados da sessão como JSON.

```bash
papecode export [sessionID]
```

Se você não fornecer um ID de sessão, será solicitado que você selecione entre as sessões disponíveis.

---

### import

Importe dados da sessão de um arquivo JSON ou URL de compartilhamento do papecode.

```bash
papecode import <file>
```

Você pode importar de um arquivo local ou de uma URL de compartilhamento do papecode.

```bash
papecode import session.json
papecode import https://opncd.ai/s/abc123
```

---

### web

Inicie um servidor papecode sem cabeça com uma interface web.

```bash
papecode web
```

Isso inicia um servidor HTTP e abre um navegador para acessar o papecode através de uma interface web. Defina `PAPECODE_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `papecode`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### acp

Inicie um servidor ACP (Agent Client Protocol).

```bash
papecode acp
```

Este comando inicia um servidor ACP que se comunica via stdin/stdout usando nd-JSON.

#### Opções

| Flag         | Descrição                 |
| ------------ | ------------------------- |
| `--cwd`      | Diretório de trabalho     |
| `--port`     | Porta para escutar        |
| `--hostname` | Nome do host para escutar |

---

### uninstall

Desinstale o papecode e remova todos os arquivos relacionados.

```bash
papecode uninstall
```

#### Opções

| Flag            | Curto | Descrição                                |
| --------------- | ----- | ---------------------------------------- |
| `--keep-config` | `-c`  | Manter arquivos de configuração          |
| `--keep-data`   | `-d`  | Manter dados de sessão e snapshots       |
| `--dry-run`     |       | Mostrar o que seria removido sem remover |
| `--force`       | `-f`  | Pular prompts de confirmação             |

---

### upgrade

Atualiza o papecode para a versão mais recente ou uma versão específica.

```bash
papecode upgrade [target]
```

Para atualizar para a versão mais recente.

```bash
papecode upgrade
```

Para atualizar para uma versão específica.

```bash
papecode upgrade v0.1.48
```

#### Opções

| Flag       | Curto | Descrição                                                        |
| ---------- | ----- | ---------------------------------------------------------------- |
| `--method` | `-m`  | O método de instalação que foi usado; curl, npm, pnpm, bun, brew |

---

## Opções Globais

A CLI do papecode aceita as seguintes flags globais.

| Flag           | Curto | Descrição                               |
| -------------- | ----- | --------------------------------------- |
| `--help`       | `-h`  | Exibir ajuda                            |
| `--version`    | `-v`  | Imprimir número da versão               |
| `--print-logs` |       | Imprimir logs no stderr                 |
| `--log-level`  |       | Nível de log (DEBUG, INFO, WARN, ERROR) |

---

## Variáveis de ambiente

O papecode pode ser configurado usando variáveis de ambiente.

| Variável                              | Tipo    | Descrição                                                             |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `PAPECODE_AUTO_SHARE`                 | boolean | Compartilhar sessões automaticamente                                  |
| `PAPECODE_GIT_BASH_PATH`              | string  | Caminho para o executável do Git Bash no Windows                      |
| `PAPECODE_CONFIG`                     | string  | Caminho para o arquivo de configuração                                |
| `PAPECODE_CONFIG_DIR`                 | string  | Caminho para o diretório de configuração                              |
| `PAPECODE_CONFIG_CONTENT`             | string  | Conteúdo de configuração json inline                                  |
| `PAPECODE_DISABLE_AUTOUPDATE`         | boolean | Desabilitar verificações de atualização automática                    |
| `PAPECODE_DISABLE_PRUNE`              | boolean | Desabilitar a poda de dados antigos                                   |
| `PAPECODE_DISABLE_TERMINAL_TITLE`     | boolean | Desabilitar atualizações automáticas do título do terminal            |
| `PAPECODE_PERMISSION`                 | string  | Configuração de permissões json inline                                |
| `PAPECODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Desabilitar plugins padrão                                            |
| `PAPECODE_DISABLE_LSP_DOWNLOAD`       | boolean | Desabilitar downloads automáticos do servidor LSP                     |
| `PAPECODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Habilitar modelos experimentais                                       |
| `PAPECODE_DISABLE_AUTOCOMPACT`        | boolean | Desabilitar compactação automática de contexto                        |
| `PAPECODE_DISABLE_CLAUDE_CODE`        | boolean | Desabilitar leitura de `.claude` (prompt + habilidades)               |
| `PAPECODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Desabilitar leitura de `~/.claude/CLAUDE.md`                          |
| `PAPECODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Desabilitar carregamento de `.claude/skills`                          |
| `PAPECODE_DISABLE_MODELS_FETCH`       | boolean | Desabilitar busca de modelos de fontes remotas                        |
| `PAPECODE_FAKE_VCS`                   | string  | Provedor VCS falso para fins de teste                                 |
| `PAPECODE_CLIENT`                     | string  | Identificador do cliente (padrão é `cli`)                             |
| `PAPECODE_ENABLE_EXA`                 | boolean | Habilitar ferramentas de busca web Exa                                |
| `PAPECODE_SERVER_PASSWORD`            | string  | Habilitar autenticação básica para `serve`/`web`                      |
| `PAPECODE_SERVER_USERNAME`            | string  | Substituir nome de usuário de autenticação básica (padrão `papecode`) |
| `PAPECODE_MODELS_URL`                 | string  | URL personalizada para buscar configuração de modelos                 |

---

### Experimental

Essas variáveis de ambiente habilitam recursos experimentais que podem mudar ou ser removidos.

| Variável                                        | Tipo    | Descrição                                                 |
| ----------------------------------------------- | ------- | --------------------------------------------------------- |
| `PAPECODE_EXPERIMENTAL`                         | boolean | Habilitar todos os recursos experimentais                 |
| `PAPECODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Habilitar descoberta de ícones                            |
| `PAPECODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Desabilitar cópia ao selecionar no TUI                    |
| `PAPECODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Tempo limite padrão para comandos bash em ms              |
| `PAPECODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Máximo de tokens de saída para respostas LLM              |
| `PAPECODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Habilitar monitoramento de arquivos para todo o diretório |
| `PAPECODE_EXPERIMENTAL_OXFMT`                   | boolean | Habilitar formatador oxfmt                                |
| `PAPECODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Habilitar ferramenta LSP experimental                     |
| `PAPECODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Desabilitar monitoramento de arquivos                     |
| `PAPECODE_EXPERIMENTAL_EXA`                     | boolean | Habilitar recursos experimentais do Exa                   |
| `PAPECODE_EXPERIMENTAL_LSP_TY`                  | boolean | Habilitar TY LSP para arquivos python                     |
| `PAPECODE_EXPERIMENTAL_MARKDOWN`                | boolean | Habilitar recursos experimentais de markdown              |
| `PAPECODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Habilitar modo de plano                                   |
