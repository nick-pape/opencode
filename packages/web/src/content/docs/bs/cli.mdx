---
title: CLI
description: PapeCode CLI opcije i naredbe.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

PapeCode CLI po defaultu pokreće [TUI](/docs/tui) kada se pokrene bez ikakvih argumenata.

```bash
papecode
```

Ali takođe prihvata naredbe kao što je dokumentovano na ovoj stranici. Ovo vam omogućava programsku interakciju sa PapeCode.

```bash
papecode run "Explain how closures work in JavaScript"
```

---

### tui

Pokrenite PapeCode terminalski korisnički interfejs.

```bash
papecode [project]
```

#### Opcije

| Opcija       | Kratko | Opis                                                                     |
| ------------ | ------ | ------------------------------------------------------------------------ |
| `--continue` | `-c`   | Nastavite posljednju sesiju                                              |
| `--session`  | `-s`   | ID sesije za nastavak                                                    |
| `--fork`     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| `--prompt`   |        | Prompt za upotrebu                                                       |
| `--model`    | `-m`   | Model za korištenje u obliku provider/model                              |
| `--agent`    |        | Agent za korištenje                                                      |
| `--port`     |        | Port na kojem treba slušati                                              |
| `--hostname` |        | Hostname na kojem treba slušati                                          |

---

## Naredbe

PapeCode CLI takođe ima sljedeće naredbe.

---

### agent

Upravljajte PapeCode agentima.

```bash
papecode agent [command]
```

---

### attach

Priključite terminal na već pokrenut PapeCode backend server pokrenut putem `serve` ili `web` naredbi.

```bash
papecode attach [url]
```

Ovo omogućava korištenje TUI-ja sa udaljenim PapeCode backend-om. Na primjer:

```bash
# Start the backend server for web/mobile access
papecode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
papecode attach http://10.20.30.40:4096
```

#### Opcije

| Opcija      | Kratko | Opis                                 |
| ----------- | ------ | ------------------------------------ |
| `--dir`     |        | Radni direktorij za pokretanje TUI-a |
| `--session` | `-s`   | ID sesije za nastavak                |

---

#### create

Kreirajte novog agenta s prilagođenom konfiguracijom.

```bash
papecode agent create
```

Ova naredba će vas voditi kroz kreiranje novog agenta sa prilagođenim sistemskim promptom i konfiguracijom alata.

---

#### list

Navedite sve dostupne agente.

```bash
papecode agent list
```

---

### auth

Naredba za upravljanje vjerodajnicama i prijavom za provajdere.

```bash
papecode auth [command]
```

---

#### login

PapeCode pokreće lista provajdera na [Models.dev](https://models.dev), tako da možete koristiti `papecode auth login` da konfigurirate API ključeve za bilo kojeg provajdera kojeg želite koristiti. Ovo je pohranjeno u `~/.local/share/papecode/auth.json`.

```bash
papecode auth login
```

Kada se PapeCode pokrene, učitava dobavljače iz datoteke vjerodajnica. I ako postoje neki ključevi definirani u vašim okruženjima ili `.env` fajl u vašem projektu.

---

#### list

Navodi sve autentifikovane dobavljače pohranjene u datoteci vjerodajnica.

```bash
papecode auth list
```

Ili kratka verzija.

```bash
papecode auth ls
```

---

#### logout

Odjavljuje vas s provajdera tako što ga briše iz datoteke vjerodajnica.

```bash
papecode auth logout
```

---

### github

Upravljajte GitHub agentom za automatizaciju repozitorija.

```bash
papecode github [command]
```

---

#### install

Instalirajte GitHub agenta u svoj repozitorij.

```bash
papecode github install
```

Ovo postavlja neophodni tok rada GitHub Actions i vodi vas kroz proces konfiguracije. [Saznajte više](/docs/github).

---

#### run

Pokrenite GitHub agent. Ovo se obično koristi u GitHub Actions.

```bash
papecode github run
```

##### Opcije

| Opcija    | Opis                                   |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event za pokretanje agenta |
| `--token` | GitHub Personal Access Token           |

---

### mcp

Upravljajte Model Context Protocol (MCP) serverima.

```bash
papecode mcp [command]
```

---

#### add

Dodajte MCP server svojoj konfiguraciji.

```bash
papecode mcp add
```

Ova naredba će vas voditi kroz dodavanje lokalnog ili udaljenog MCP servera.

---

#### list

Navedite sve konfigurirane MCP servere i njihov status veze.

```bash
papecode mcp list
```

Ili koristite kratku verziju.

```bash
papecode mcp ls
```

---

#### auth

Autentifikujte se sa MCP serverom koji je omogućen za OAuth.

```bash
papecode mcp auth [name]
```

Ako ne navedete ime servera, od vas će biti zatraženo da izaberete neki od dostupnih servera koji podržavaju OAuth.
Također možete navesti servere koji podržavaju OAuth i njihov status autentifikacije.

```bash
papecode mcp auth list
```

Ili koristite kratku verziju.

```bash
papecode mcp auth ls
```

---

#### logout

Uklonite OAuth vjerodajnice za MCP server.

```bash
papecode mcp logout [name]
```

---

#### debug

Otklanjanje grešaka (debug) OAuth veze sa MCP serverom.

```bash
papecode mcp debug <name>
```

---

### models

Navedite sve dostupne modele konfiguriranih provajdera.

```bash
papecode models [provider]
```

Ova naredba prikazuje sve modele dostupne kod vaših konfiguriranih provajdera u formatu `provider/model`.
Ovo je korisno za pronalaženje tačnog naziva modela za korištenje u [vašoj konfiguraciji](/docs/config/).
Opciono možete proslijediti ID provajdera za filtriranje modela po tom dobavljaču.

```bash
papecode models anthropic
```

#### Opcije

| Opcija      | Opis                                                                     |
| ----------- | ------------------------------------------------------------------------ |
| `--refresh` | Osvježite keš modela sa models.dev                                       |
| `--verbose` | Koristite detaljniji izlaz modela (uključuje metapodatke poput troškova) |

Koristite `--refresh` zastavicu da ažurirate keširanu listu modela. Ovo je korisno kada su novi modeli dodani provajderu i želite da ih vidite u PapeCode.

```bash
papecode models --refresh
```

---

### run

Pokrenite PapeCode u neinteraktivnom modu tako što ćete direktno proslijediti prompt.

```bash
papecode run [message..]
```

Ovo je korisno za skriptiranje, automatizaciju ili kada želite brz odgovor bez pokretanja punog TUI-ja. Na primjer:

```bash "papecode run"
papecode run Explain the use of context in Go
```

Također možete priključiti pokrenutu `papecode serve` instancu kako biste izbjegli vrijeme hladnog pokretanja MCP servera pri svakom pokretanju:

```bash
# Start a headless server in one terminal
papecode serve

# In another terminal, run commands that attach to it
papecode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opcije

| Opcija       | Kratko | Opis                                                                     |
| ------------ | ------ | ------------------------------------------------------------------------ |
| `--command`  |        | Naredba za pokretanje, koristite poruku za argumente                     |
| `--continue` | `-c`   | Nastavite posljednju sesiju                                              |
| `--session`  | `-s`   | ID sesije za nastavak                                                    |
| `--fork`     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| `--share`    |        | Podijelite sesiju                                                        |
| `--model`    | `-m`   | Model za korištenje u obliku provider/model                              |
| `--agent`    |        | Agent za korištenje                                                      |
| `--file`     | `-f`   | Fajlovi koje treba priložiti poruci                                      |
| `--format`   |        | Format: default (formatiran) ili json (sirovi JSON događaji)             |
| `--title`    |        | Naslov sesije (koristi skraćeni prompt ako nije navedena vrijednost)     |
| `--attach`   |        | Priključite na pokrenuti PapeCode server (npr. http://localhost:4096)    |
| `--port`     |        | Port za lokalni server (zadano na nasumični port)                        |

---

### serve

Pokrenite PapeCode headless server za API pristup. Pogledajte [server docs](/docs/server) za kompletan HTTP interfejs.

```bash
papecode serve
```

Ovo pokreće HTTP server koji pruža API pristup funkcionalnosti PapeCode-a bez TUI interfejsa. Postavite `PAPECODE_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `papecode`).

#### Opcije

| Opcija       | Opis                                                  |
| ------------ | ----------------------------------------------------- |
| `--port`     | Port na kojem treba slušati                           |
| `--hostname` | Hostname na kojem treba slušati                       |
| `--mdns`     | Omogući mDNS otkrivanje                               |
| `--cors`     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### session

Upravljajte PapeCode sesijama.

```bash
papecode session [command]
```

---

#### list

Navedite sve PapeCode sesije.

```bash
papecode session list
```

##### Opcije

| Opcija        | Kratko | Opis                                   |
| ------------- | ------ | -------------------------------------- |
| `--max-count` | `-n`   | Ograničenje na N najnovijih sesija     |
| `--format`    |        | Izlazni format: table ili json (table) |

---

### stats

Prikaži statistiku upotrebe tokena i troškova za vaše PapeCode sesije.

```bash
papecode stats
```

#### Opcije

| Opcija      | Opis                                                                                                       |
| ----------- | ---------------------------------------------------------------------------------------------------------- |
| `--days`    | Prikaži statistiku za zadnjih N dana (sva vremena)                                                         |
| `--tools`   | Broj alata za prikaz (svi)                                                                                 |
| `--models`  | Prikaži raščlambu korištenja modela (skriveno prema zadanim postavkama). Proslijedite broj za prikaz top N |
| `--project` | Filtriraj po projektu (svi projekti, prazan niz: trenutni projekt)                                         |

---

### export

Izvezite podatke sesije kao JSON.

```bash
papecode export [sessionID]
```

Ako ne unesete ID sesije, od vas će biti zatraženo da odaberete neku od dostupnih sesija.

---

### import

Uvezite podatke sesije iz JSON datoteke ili PapeCode dijeljenog URL-a.

```bash
papecode import <file>
```

Možete uvesti iz lokalne datoteke ili PapeCode dijeljenog URL-a.

```bash
papecode import session.json
papecode import https://opncd.ai/s/abc123
```

---

### web

Pokrenite PapeCode headless server sa web interfejsom.

```bash
papecode web
```

Ovo pokreće HTTP server i otvara web pretraživač za pristup PapeCode-u preko web interfejsa. Postavite `PAPECODE_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `papecode`).

#### Opcije

| Opcija       | Opis                                                  |
| ------------ | ----------------------------------------------------- |
| `--port`     | Port na kojem treba slušati                           |
| `--hostname` | Hostname na kojem treba slušati                       |
| `--mdns`     | Omogući mDNS otkrivanje                               |
| `--cors`     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### acp

Pokrenite ACP (Agent Client Protocol) server.

```bash
papecode acp
```

Ova naredba pokreće ACP server koji komunicira preko stdin/stdout koristeći nd-JSON.

#### Opcije

| Opcija       | Opis                        |
| ------------ | --------------------------- |
| `--cwd`      | Radni direktorij            |
| `--port`     | Port na kojem treba slušati |
| `--hostname` | Hostname na kojem slušati   |

---

### uninstall

Deinstalirajte PapeCode i uklonite sve povezane datoteke.

```bash
papecode uninstall
```

#### Opcije

| Opcija          | Kratko | Opis                                          |
| --------------- | ------ | --------------------------------------------- |
| `--keep-config` | `-c`   | Sačuvajte konfiguracijske datoteke            |
| `--keep-data`   | `-d`   | Sačuvajte podatke i snimke sesije             |
| `--dry-run`     |        | Pokažite šta bi bilo uklonjeno bez uklanjanja |
| `--force`       | `-f`   | Preskoči upite za potvrdu                     |

---

### upgrade

Ažurira PapeCode na najnoviju verziju ili određenu verziju.

```bash
papecode upgrade [target]
```

Za nadogradnju na najnoviju verziju.

```bash
papecode upgrade
```

Za nadogradnju na određenu verziju.

```bash
papecode upgrade v0.1.48
```

#### Opcije

| Opcija     | Kratko | Opis                                                    |
| ---------- | ------ | ------------------------------------------------------- |
| `--method` | `-m`   | Korišteni način instalacije; curl, npm, pnpm, bun, brew |

---

## Globalne opcije

PapeCode CLI prihvata sljedeće globalne zastavice.

| Opcija         | Kratko | Opis                                      |
| -------------- | ------ | ----------------------------------------- |
| `--help`       | `-h`   | Prikaži pomoć                             |
| `--version`    | `-v`   | Ispiši broj verzije                       |
| `--print-logs` |        | Ispis logova u stderr                     |
| `--log-level`  |        | Nivo logovanja (DEBUG, INFO, WARN, ERROR) |

---

## Varijable okruženja

PapeCode se može konfigurirati pomoću varijabli okruženja.

| Varijabla                             | Tip     | Opis                                                               |
| ------------------------------------- | ------- | ------------------------------------------------------------------ |
| `PAPECODE_AUTO_SHARE`                 | boolean | Automatski dijeli sesije                                           |
| `PAPECODE_GIT_BASH_PATH`              | string  | Putanja do Git Bash izvršne datoteke na Windows-u                  |
| `PAPECODE_CONFIG`                     | string  | Putanja do konfiguracijskog fajla                                  |
| `PAPECODE_TUI_CONFIG`                 | string  | Putanja do TUI konfiguracijskog fajla                              |
| `PAPECODE_CONFIG_DIR`                 | string  | Putanja do konfiguracijskog direktorija                            |
| `PAPECODE_CONFIG_CONTENT`             | string  | Inline json konfiguracijski sadržaj                                |
| `PAPECODE_DISABLE_AUTOUPDATE`         | boolean | Onemogući automatske provjere ažuriranja                           |
| `PAPECODE_DISABLE_PRUNE`              | boolean | Onemogući brisanje (pruning) starih podataka                       |
| `PAPECODE_DISABLE_TERMINAL_TITLE`     | boolean | Onemogući automatsko ažuriranje naslova terminala                  |
| `PAPECODE_PERMISSION`                 | string  | Inline json konfiguracija dozvola                                  |
| `PAPECODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Onemogući podrazumijevane dodatke (plugins)                        |
| `PAPECODE_DISABLE_LSP_DOWNLOAD`       | boolean | Onemogući automatsko preuzimanje LSP servera                       |
| `PAPECODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Omogući eksperimentalne modele                                     |
| `PAPECODE_DISABLE_AUTOCOMPACT`        | boolean | Onemogući automatsko sažimanje konteksta                           |
| `PAPECODE_DISABLE_CLAUDE_CODE`        | boolean | Onemogući čitanje iz `.claude` (prompt + vještine)                 |
| `PAPECODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Onemogući čitanje `~/.claude/CLAUDE.md`                            |
| `PAPECODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Onemogući učitavanje `.claude/skills`                              |
| `PAPECODE_DISABLE_MODELS_FETCH`       | boolean | Onemogući dohvaćanje modela iz udaljenih izvora                    |
| `PAPECODE_FAKE_VCS`                   | string  | Lažni VCS provajder za potrebe testiranja                          |
| `PAPECODE_CLIENT`                     | string  | Identifikator klijenta (zadano na `cli`)                           |
| `PAPECODE_ENABLE_EXA`                 | boolean | Omogući Exa alate za web pretraživanje                             |
| `PAPECODE_SERVER_PASSWORD`            | string  | Omogući osnovnu autentifikaciju za `serve`/`web`                   |
| `PAPECODE_SERVER_USERNAME`            | string  | Poništi osnovno korisničko ime autentifikacije (zadano `papecode`) |
| `PAPECODE_MODELS_URL`                 | string  | Prilagođeni URL za dohvaćanje konfiguracije modela                 |

---

### Eksperimentalno

Ove varijable okruženja omogućavaju eksperimentalne karakteristike koje se mogu promijeniti ili ukloniti.

| Varijabla                                       | Tip     | Opis                                              |
| ----------------------------------------------- | ------- | ------------------------------------------------- |
| `PAPECODE_EXPERIMENTAL`                         | boolean | Omogući sve eksperimentalne funkcije              |
| `PAPECODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Omogući otkrivanje ikona                          |
| `PAPECODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Onemogući kopiranje pri odabiru u TUI             |
| `PAPECODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Zadano vremensko ograničenje za bash naredbe u ms |
| `PAPECODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksimalni izlazni tokeni za LLM odgovore         |
| `PAPECODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Omogući praćenje datoteka za cijeli direktorij    |
| `PAPECODE_EXPERIMENTAL_OXFMT`                   | boolean | Omogući oxfmt formatter                           |
| `PAPECODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Omogući eksperimentalni LSP alat                  |
| `PAPECODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Onemogući praćenje datoteka                       |
| `PAPECODE_EXPERIMENTAL_EXA`                     | boolean | Omogući eksperimentalne Exa funkcije              |
| `PAPECODE_EXPERIMENTAL_LSP_TY`                  | boolean | Omogući TY LSP za python datoteke                 |
| `PAPECODE_EXPERIMENTAL_MARKDOWN`                | boolean | Omogući eksperimentalne Markdown funkcije         |
| `PAPECODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Omogući Plan mod                                  |
